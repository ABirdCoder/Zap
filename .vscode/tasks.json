{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "debug",
			"type": "cppbuild",
			"command": "/usr/bin/g++",
      "args": ["${workspaceFolder}/src/*.cpp", "-I", "include", "-o", "${workspaceFolder}/main"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
		},
    {
      "label": "run",
      "type": "shell",
      "command": "${workspaceFolder}/main"
    }
	]
}